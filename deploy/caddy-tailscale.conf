# TODO(tailscale): Caddy reverse proxy config for Tailscale exposure
#
# Prerequisites:
#   1. Install Caddy: sudo apt install caddy
#   2. Enable Tailscale: tailscale up
#   3. Get your Tailscale hostname: tailscale status | head -1
#   4. Replace YOUR_MACHINE.YOUR_TAILNET.ts.net below
#   5. Change OPENBRAIN_WEB_HOST=0.0.0.0 in .env
#   6. Copy to /etc/caddy/Caddyfile (or include from it)
#   7. sudo systemctl restart caddy openbrain-web@$USER
#
# Access: https://YOUR_MACHINE.YOUR_TAILNET.ts.net (Caddy auto-TLS via Let's Encrypt)

# mybrain.YOUR_TAILNET.ts.net {
#     reverse_proxy 127.0.0.1:10203
#
#     # WebSocket support (required for the chat UI)
#     @websocket {
#         header Connection *Upgrade*
#         header Upgrade websocket
#     }
#     reverse_proxy @websocket 127.0.0.1:10203
#
#     # Optional: restrict to Tailscale IP range only
#     # @tailscale {
#     #     remote_ip 100.64.0.0/10
#     # }
#     # handle @tailscale {
#     #     reverse_proxy 127.0.0.1:10203
#     # }
#     # respond "Forbidden" 403
# }
