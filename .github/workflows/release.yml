# Release workflow for openbrain (WD-012, OB-040).
#
# TRIGGER MODEL: deliberate dispatch only.
#   Releases are cut by running this workflow manually:
#
#     Real release:  gh workflow run release.yml --repo windingriverholdings/openbrain -f dry_run=false
#     Smoke check:   gh workflow run release.yml --repo windingriverholdings/openbrain -f dry_run=true
#     Bare/UI dispatch (default): dry run, harmless.
#
#   dry_run defaults to true. A bare Actions-UI dispatch or any invocation
#   without -f dry_run=false is a harmless smoke check. An explicit
#   -f dry_run=false is required to cut a real release. This prevents
#   accidental UI dispatches from creating tags or GitHub releases.
#
#   There is NO push-to-main auto-trigger. Merging a PR to main does NOT
#   cut a release. Craig decides when to release; Kit dispatches the workflow.
#
# NO PUSH TO refs/heads/main (releasing.md sections 3, 4, 5):
#   The main branch is protected (ruleset 16819112: changes must go through a
#   pull request). semantic-release therefore does NOT rewrite a source version
#   constant and does NOT push a bump commit to main. Its only outputs are the
#   git tag and the GitHub release, both from @semantic-release/github. See
#   .releaserc.js for the trimmed plugin chain.
#
#   semantic-release core DOES push the tag, but the push targets refs/tags/*
#   (the tag ruleset restricts update and deletion, not creation), and it is
#   authenticated by a token-bearing remote URL that semantic-release builds
#   from GITHUB_TOKEN, not by the checkout's ambient stored credentials. That
#   is why persist-credentials below is false: the tag push does not need them
#   and no commit ever targets refs/heads/main.
#
#   The version is stamped into the binary at build time via ldflags from git
#   describe (see the Makefile). An un-injected build reports the "dev"
#   sentinel. The canary smoke-test below verifies the ldflags symbol path is
#   wired correctly before a tag is ever created: a typo in the -X path would
#   silently yield "dev" in production while every unit test stays green.
#
# RUNNER POLICY (devops.md, OB-039):
#   runs-on: ubuntu-latest. openbrain is a PUBLIC repo and the org's only
#   self-hosted runner group denies public repos (allows_public_repositories:
#   false), so this workflow could never be dispatched on self-hosted: it
#   queued forever. This job is NOT deploy-bearing (deploy_target: none, no
#   SSH, no DockerHub push): it only creates a tag and a GitHub release via
#   GITHUB_TOKEN, so the devops.md public-repo/non-deploy-bearing carve-out
#   applies, matching test.yml's runner choice.
#
# CONCURRENCY:
#   cancel-in-progress: false: if an operator accidentally dispatches twice,
#   the second run queues rather than cancelling the first mid-flight. A
#   cancelled mid-flight release is harder to recover from than a queued one.
#
# GATES (releasing.md section 7):
#   The gates in project.yml release.gates (unit-tests, reviews-complete,
#   cve-gate) are REQUIRED status checks in the openbrain main branch ruleset.
#   Kit verifies the gate checks are green before dispatching this workflow.
#   The workflow itself does not replicate gate logic in `needs:`.

name: Release

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run semantic-release in dry-run mode (no tag, no release)"
        type: boolean
        default: true

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  # false: queue a second manual dispatch rather than cancel a run mid-flight.
  cancel-in-progress: false

permissions:
  contents: write       # required: @semantic-release/github creates the tag and release
  issues: write         # required: @semantic-release/github creates/closes release issues
  pull-requests: write  # required: @semantic-release/github comments on PRs

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (full history)
        uses: actions/checkout@v7
        with:
          # fetch-depth: 0 is required. semantic-release reads the full tag
          # history to compute the next version. A shallow clone produces an
          # incorrect or missing bump. git describe (for the canary and the
          # deploy build) also needs the tag history.
          fetch-depth: 0
          # persist-credentials: false: the release makes NO commit to
          # refs/heads/main. semantic-release pushes only the tag, and it
          # authenticates that push with a token-bearing remote URL it builds
          # from GITHUB_TOKEN, so the checkout's ambient stored credentials are
          # not needed here.
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          # Node >= 22 required by the shared config's engines field.
          node-version: '22'

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: false

      - name: Install semantic-release and shared config
        # npm ci installs from package-lock.json (locked transitive graph).
        # The shared config is installed from GitHub at the pinned #v0.2.0 tag.
        run: npm ci

      - name: Assert forge token is set (pre-flight)
        # A missing or empty GITHUB_TOKEN causes the github plugin to fail AFTER
        # semantic-release has computed and logged the release, making diagnosis
        # harder. This check aborts before any plugin step runs.
        run: |
          set -euo pipefail
          if [ -z "${GITHUB_TOKEN}" ]; then
            echo "ERROR: GITHUB_TOKEN is empty. Aborting before release step runs." >&2
            exit 1
          fi
          echo "OK: GITHUB_TOKEN is set."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (pre-flight sanity)
        # Confirm the Go module compiles cleanly before the canary and release.
        # A broken build before the release step prevents a tag from being
        # created for code that does not compile.
        run: |
          set -euo pipefail
          go build ./...

      - name: Canary version smoke-test (releasing.md section 4)
        # Gate the tag on proof that the ldflags -X symbol path is wired
        # correctly. Build the CLI with a synthetic, non-semver sentinel
        # injected via the SAME ldflags path the Makefile uses, boot it with
        # zero dependencies (the --version flag runs before any config or DB
        # load), and assert it reports EXACTLY the sentinel. A "dev" result or
        # any mismatch means the -X path is wrong: hard-fail before a tag
        # exists. Unit tests cannot catch this: they never receive the stamp.
        run: |
          set -euo pipefail
          SENTINEL="v0.0.0-ldflags-smoke-test"
          SYMBOL="github.com/windingriverholdings/openbrain/internal/version.Version"
          CANARY_DIR="$(mktemp -d)"
          CANARY_BIN="${CANARY_DIR}/openbrain-canary"
          go build -ldflags "-X ${SYMBOL}=${SENTINEL}" -o "${CANARY_BIN}" ./cmd/openbrain
          REPORTED="$("${CANARY_BIN}" --version)"
          rm -rf "${CANARY_DIR}"
          if [ "${REPORTED}" != "${SENTINEL}" ]; then
            echo "ERROR: canary reported version '${REPORTED}', expected '${SENTINEL}'." >&2
            echo "The ldflags -X symbol path '${SYMBOL}' is wrong or the accessor changed." >&2
            echo "A tagged production build would silently report 'dev'. Aborting before tag." >&2
            exit 1
          fi
          echo "OK: canary reported '${REPORTED}'. The ldflags symbol path is wired correctly."

      - name: Dist release-asset build smoke-test (releasing.md section 4, OB-043)
        # The real asset build runs in semantic-release's PREPARE step, which
        # semantic-release SKIPS on --dry-run (prepare is defined dryRun:false in
        # semantic-release/lib/definitions/plugins.js). So the default
        # dry_run=true smoke check never exercises `make dist`: the six
        # cross-compiles and the sha256sum step would first run only on a real
        # release, where a failure aborts AFTER the version is computed. This
        # ALWAYS-RUN step (not gated on dry_run, like the ldflags canary above)
        # exercises the exact dist build with a synthetic version, asserts the
        # six binaries AND SHA256SUMS were produced, then removes dist/ so this
        # smoke build does not pollute the real prepare build that runs later.
        run: |
          set -euo pipefail
          SMOKE_VERSION="v0.0.0-dist-smoke-test"
          make dist DIST_VERSION="${SMOKE_VERSION}"
          MISSING=0
          for cmd in openbrain openbrain-mcp openbrain-web openbrain-telegram openbrain-slack openbrain-watchd; do
            ASSET="dist/${cmd}-${SMOKE_VERSION}-linux-amd64"
            if [ ! -f "${ASSET}" ]; then
              echo "ERROR: expected dist asset '${ASSET}' was not produced." >&2
              MISSING=1
            fi
          done
          if [ ! -f dist/SHA256SUMS ]; then
            echo "ERROR: dist/SHA256SUMS was not produced." >&2
            MISSING=1
          fi
          rm -rf dist
          if [ "${MISSING}" -ne 0 ]; then
            echo "make dist did not produce the full asset set. Aborting before release." >&2
            exit 1
          fi
          echo "OK: make dist produced the six binaries and SHA256SUMS."

      # id: release lets the later steps address this step's outputs. It does
      # NOT create those outputs: the step body below writes
      # new_release_published and new_release_version to $GITHUB_OUTPUT itself
      # by parsing semantic-release's log, because `npx semantic-release` is a
      # plain CLI and never populates step outputs on its own.
      #
      # DRY_RUN is set via env (not inline shell interpolation) so the boolean
      # input never touches the shell command string directly.
      - name: Release
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail
          LOG="$(mktemp)"
          if [ "${DRY_RUN}" = "true" ]; then
            npx semantic-release --dry-run 2>&1 | tee "${LOG}"
          else
            npx semantic-release 2>&1 | tee "${LOG}"
          fi
          # semantic-release core logs "Published release <version> on <channel>
          # channel" even in --dry-run: in semantic-release 25.0.5 that
          # logger.success call sits AFTER the dry-run branch, so dry-run skips
          # only the tag and push, not the log line. The log match alone is
          # therefore NOT a real-publish signal. The published detection must
          # also gate on DRY_RUN != "true": a dry-run never creates a tag, so it
          # must always take the harmless no-assert path or the tag-exists assert
          # below would hard-fail every smoke check that has a pending commit.
          # Strip ANSI color codes first so a colored CI log still matches. The
          # extraction reads the completed tee'd LOG file (no live pipe), so
          # tail -n1 is not about SIGPIPE: it just takes the last match if the
          # line appeared more than once.
          VERSION="$(sed -e 's/\x1b\[[0-9;]*m//g' "${LOG}" \
            | sed -n 's/.*Published release \([0-9][0-9A-Za-z.+-]*\).*/\1/p' \
            | tail -n1)"
          if [ "${DRY_RUN}" != "true" ] && [ -n "${VERSION}" ]; then
            echo "new_release_published=true" >> "${GITHUB_OUTPUT}"
            echo "new_release_version=${VERSION}" >> "${GITHUB_OUTPUT}"
            echo "Detected published release version: ${VERSION}"
          else
            echo "new_release_published=false" >> "${GITHUB_OUTPUT}"
            if [ "${DRY_RUN}" = "true" ]; then
              echo "Dry-run: no tag created, skipping the post-release tag assert."
            else
              echo "No 'Published release' line found: no tag was created this run."
            fi
          fi
          rm -f "${LOG}"

      - name: No release produced (informational)
        if: steps.release.outputs.new_release_published != 'true'
        run: |
          set -euo pipefail
          echo "No release was published this run. This is expected in dry-run mode,"
          echo "and also when no feat/fix/perf commits exist since the last tag. If a"
          echo "real release was expected, inspect the log above: non-conventional"
          echo "commit messages are not counted as releasable."

      - name: Assert release tag exists on remote (post-release)
        # Fires only when a real release was published (parsed above). Confirms
        # the tag semantic-release reported actually landed on the remote. This
        # is the section 5 assert that distinguishes "release created" from "ran
        # cleanly but created nothing": a silent tag-push failure that still
        # exited zero is caught here. TAG_VERSION is passed via env to avoid
        # shell-interpolation surprises on unexpected output shapes.
        if: steps.release.outputs.new_release_published == 'true'
        env:
          TAG_VERSION: ${{ steps.release.outputs.new_release_version }}
        run: |
          set -euo pipefail
          TAG="v${TAG_VERSION}"
          if ! git ls-remote --tags origin "refs/tags/${TAG}" | grep -q "${TAG}"; then
            echo "ERROR: expected tag '${TAG}' not found on remote after release." >&2
            echo "semantic-release reported a publish but the tag is not on origin;" >&2
            echo "the tag push may have failed silently. Failing the run loudly." >&2
            exit 1
          fi
          echo "OK: release tag '${TAG}' confirmed on remote."

      - name: Assert release assets attached (post-release, section 5, OB-043)
        # @semantic-release/github@12.0.8 publish() SWALLOWS a missing or
        # unreadable asset: on a failed stat it logs "The asset %s cannot be
        # read, and will be ignored" and returns WITHOUT rejecting Promise.all,
        # so the release can finalize with FEWER assets than intended. The
        # tag-exists assert above never sees this. This step confirms the
        # release carries EXACTLY the seven expected assets (the six binaries +
        # SHA256SUMS), failing loud on any count or name mismatch. It extends the
        # section-5 "did the release do the right thing" guarantee to the assets
        # this workflow exists to ship. TAG_VERSION is passed via env; gh reads
        # GITHUB_TOKEN from the environment.
        if: steps.release.outputs.new_release_published == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_VERSION: ${{ steps.release.outputs.new_release_version }}
        run: |
          set -euo pipefail
          TAG="v${TAG_VERSION}"
          EXPECTED_SORTED="$(printf '%s\n' \
            "openbrain-${TAG}-linux-amd64" \
            "openbrain-mcp-${TAG}-linux-amd64" \
            "openbrain-web-${TAG}-linux-amd64" \
            "openbrain-telegram-${TAG}-linux-amd64" \
            "openbrain-slack-${TAG}-linux-amd64" \
            "openbrain-watchd-${TAG}-linux-amd64" \
            "SHA256SUMS" | sort)"
          ACTUAL_SORTED="$(gh release view "${TAG}" --json assets --jq '.assets[].name' | sort)"
          ACTUAL_COUNT="$(printf '%s\n' "${ACTUAL_SORTED}" | grep -c . || true)"
          if [ "${ACTUAL_COUNT}" -ne 7 ] || [ "${ACTUAL_SORTED}" != "${EXPECTED_SORTED}" ]; then
            echo "ERROR: release ${TAG} assets do not match the expected seven." >&2
            echo "Expected:" >&2
            printf '%s\n' "${EXPECTED_SORTED}" >&2
            echo "Actual (count ${ACTUAL_COUNT}):" >&2
            printf '%s\n' "${ACTUAL_SORTED}" >&2
            echo "@semantic-release/github may have silently ignored an unreadable asset." >&2
            exit 1
          fi
          echo "OK: release ${TAG} carries all seven expected assets."
